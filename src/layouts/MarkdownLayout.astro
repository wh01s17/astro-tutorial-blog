---
import Footer from "../components/Footer.astro";
const { frontmatter } = Astro.props;
---

<h1>{frontmatter.title}</h1>
<p>Escrito por:{frontmatter.author} en {frontmatter.pubDate.slice(0, 10)}</p>
<slot />
<Footer />
